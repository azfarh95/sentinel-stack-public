fastapi>=0.104.0
fastmcp>=0.5.0
httpx>=0.24.0
uvicorn>=0.24.0
starlette>=0.36.0
# PDF → LLM-ready Markdown (built on PyMuPDF). AGPL-3.0 — fine for local use.
pymupdf4llm>=0.0.17
PyMuPDF>=1.24.0
# RapidOCR (upgrade OCR tier) — PaddleOCR's PP-OCR models on ONNXRuntime, CPU-
# friendly, models bundled in the wheel (no runtime download). Better on poor
# scans / rotation / dense layout than Tesseract. Apache-2.0.
rapidocr_onnxruntime>=1.3.0
onnxruntime>=1.17.0

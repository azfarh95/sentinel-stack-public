# 🌅 V6 Prep — 4 improvements before next benchmark

You queued these last night (2026-05-09) after the V3 wishlist research benchmark surfaced systemic issues. Run them now while you're home with focus.

## The 4 tasks

### 1. ✅ DONE 2026-05-09 evening — OpenClaw compaction + LM Studio context bump

**Two related changes shipped together:**

**a) OpenClaw `agents.defaults.compaction`** (in `~/.openclaw/openclaw.json`):
```jsonc
"compaction": {
  "reserveTokensFloor": 30000,
  "keepRecentTokens": 8000,
  "truncateAfterCompaction": true,
  "maxActiveTranscriptBytes": 1500000
}
```
Hot-reloaded via SIGUSR1.

**b) LM Studio context bumped 98K → 128K** (3 layers synced):
- Runtime: `lms unload && lms load qwen/qwen3.6-27b -c 131072 -y`
- Saved default: `~/.lmstudio/.internal/user-concrete-model-default-config/qwen/qwen3.6-27b.json` → `llm.load.contextLength: 131072`
- OpenClaw `models.providers.lmstudio.models[].contextTokens: 131072` (auto-synced via `scripts/sync_lm_models.py`)

**Net effect**: compaction trigger moved from ~64K (overly tight at 98K context) to **~94K** (= 128K - 30K reserve - 4K soft) — much more headroom, while still being aggressive enough to prevent today's stuck-loop pattern.

**VRAM impact**: ~22-23 GB used on 24 GB card. Tight but stable.

**To reproduce on a fresh install** (V6 portability concern): need to apply both changes during first-run wizard. The OpenClaw config is per-user (`~/.openclaw/openclaw.json`); the LM Studio default is per-machine (`~/.lmstudio/.internal/...`). Bootstrap script should template both.

Skip this task tomorrow.

### 2. Expand Sentinel's research-source hint list
**Why**: Sentinel cited 9 sources, all generalist business/UX blogs. Missed every self-hosted-AI community voice. Claude found those easily on the second pass.
**How**: edit `\\wsl.localhost\Ubuntu-24.04\home\azfar\.openclaw\workspace\TOOLS.md` — add a section to the Research / Web tools area that lists: r/selfhosted, r/LocalLLaMA, Hacker News (especially Show HN), project roadmaps for mem0, Letta, Open WebUI, AnythingLLM, Home Assistant Voice PE.
**Estimate**: 15 min.

### 3. Add length-calibration to research-task brief template
**Why**: Sentinel produced 9 pages without being asked for a target. Could just as easily have been 2 pages or 30. Predictability matters for capacity planning.
**How**: in TOOLS.md add a Research workflow section: "When given an open-ended research mission, ask for or estimate a target page count (3-10 pages typical) and aim for that ±20%. Note actual length in your reply."
**Estimate**: 15 min.

### 4. Build self-correction for "announced but didn't write" pattern
**Why**: Sentinel said "Now I have substantial material, let me write the doc" twice before actually writing it on the third try. Cost ~4 minutes + ~140K wasted input tokens.
**How**: in TOOLS.md add a discipline rule: "After announcing a file write, on the very next turn verify the file exists with the expected content. If not, write it immediately rather than continuing other work."
**Estimate**: 1-2 hours including testing.

## After all 4 land

Re-run a similar research mission and compare against the 2026-05-09 baseline. The benchmark database is at `workspace/benchmarks/benchmarks.yaml > 2026-05-09-v3-wishlist-sentinel`.

Use `scripts/extract_benchmark.py` to generate the new entry. Then eyeball whether:
- Wall-clock dropped (compaction → less wasted prefill)
- Peak input per turn stayed under 80K (compaction working)
- Output still ~9 pages or as targeted (length calibration working)
- No false-start turns (self-correction working)

If any of those didn't improve, investigate before adding more tasks to the queue.

Have a good morning ☕

— Claude (yesterday's Claude)

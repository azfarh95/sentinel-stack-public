# `_archive/` — retired-but-preserved trees

Things that have been **deprecated and archived, not deleted**. Nothing here is
live; nothing here should be run without reading its own notice first.

| Folder | What | Retired | Replaced by |
|---|---|---|---|
| `watchdog-v1/` | Sentinel Watchdog **v1** monolith (`watchdog.py`) — a nested git repo, history preserved | 2026-05-26 (E.1); archived 2026-06-21 (V3.5) | `C:\Users\azfar\sentinel-watchdog` (v2) — see `watchdog-v1/DEPRECATED.md` |

`watchdog-v1/` keeps its own `.git`, so it's left **untracked** by `metamcp-local`
(see `.gitignore`) — the bits and history are preserved on disk and in that
nested repo, just out of the active tree.

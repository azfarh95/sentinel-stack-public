fastapi>=0.104.0
fastmcp>=0.5.0
httpx>=0.24.0
uvicorn>=0.24.0
starlette>=0.36.0
# RapidOCR — PaddleOCR's PP-OCR models on ONNXRuntime, CPU-friendly, models
# bundled in the wheel (no runtime download). The Phase-1 OcrEngine. Apache-2.0.
rapidocr_onnxruntime>=1.3.0
onnxruntime>=1.17.0
# P1b fallback OcrEngine (auto-routed for non-Latin / low-confidence inputs).
# The tesseract binary + lang packs come from the Dockerfile (apt).
pytesseract>=0.3.10
# Image handling (downscale huge phone photos before OCR) + ndarray for RapidOCR.
pillow>=10.0.0
numpy>=1.26.0
# read_document: PDF/EPUB text (PyMuPDF) + our own RapidOCR for scanned pages.
# (pymupdf4llm dropped — its bundled OCR is version-incompatible with RapidOCR.)
PyMuPDF>=1.24.0
